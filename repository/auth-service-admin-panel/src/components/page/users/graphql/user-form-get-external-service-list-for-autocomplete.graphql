query UserFormGetExternalServiceListForAutocomplete {
    externalService {
        list (input: {perPage: 1000}) {
            data {
                id
                name
            }
        }
    }
}