mutation RegistrationPageExternalServiceSignUp($input: TExternalServiceSignUpInput!) {
    auth {
        externalServiceSignUp(input: $input) {
            accessToken
            accessTokenTTL
            refreshToken
            refreshTokenTTL
        }
    }
}