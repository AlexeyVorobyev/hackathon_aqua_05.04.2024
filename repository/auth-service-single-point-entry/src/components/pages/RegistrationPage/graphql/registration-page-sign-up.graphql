mutation RegistrationPageSignUp($input: TSignUpInput!) {
    auth {
        baseSignUp(input: $input) {
            accessToken
            accessTokenTTL
            refreshToken
            refreshTokenTTL
        }
    }
}