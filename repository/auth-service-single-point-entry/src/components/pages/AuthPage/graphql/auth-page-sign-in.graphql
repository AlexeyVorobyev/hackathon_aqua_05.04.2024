query AuthPageSignIn($input: TSignInInput!) {
    auth {
        signIn(input: $input) {
            accessToken
            accessTokenTTL
            refreshToken
            refreshTokenTTL
        }
    }
}